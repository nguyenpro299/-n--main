<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lab6_23520536_21521202">

        <!-- Splash -->
        <activity
            android:name=".avtivity.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding -->
        <activity android:name=".avtivity.OnboardingActivity" />

        <!-- Login -->
        <activity android:name=".auth.LoginActivity" />

        <!-- Main -->
        <activity android:name=".avtivity.MainActivity" />
        <activity android:name=".avtivity.SelectDepartmentActivity" />
        <activity android:name=".avtivity.SelectDateTimeActivity" />
        <activity android:name=".avtivity.SelectProfileActivity" />
        <activity android:name=".avtivity.PaymentActivity" />
    </application>

</manifest>